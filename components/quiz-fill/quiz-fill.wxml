<view class="quiz-fill">
  <input
    class="fill-input"
    type="text"
    placeholder="{{question.placeholder || '请输入答案'}}"
    value="{{inputValue}}"
    bindinput="onInput"
    bindconfirm="onConfirm"
  />
  <text class="fill-hint" wx:if="{{question.hint}}">{{question.hint}}</text>
</view>