# 2026-05-22-公园介绍功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有的推荐观鸟点页面中增加公园介绍功能，通过标签页(Tab)形式展示公园的概览、鸟类信息和生态环境等内容

**Architecture:** 扩展现有的 birding-spots 页面，在不重构现有代码的前提下，添加标签页导航和详细公园信息展示功能。从现有的鸟类数据中提取公园相关信息，并使用标签页形式展示不同类别的信息。

**Tech Stack:** 微信小程序框架，WXML/WXSS/JS

---

## 文件结构规划

- `miniprogram/pages/birding-spots/birding-spots.wxml` - 扩展页面结构，添加标签页导航和内容区域
- `miniprogram/pages/birding-spots/birding-spots.js` - 添加标签页管理和公园数据处理逻辑
- `miniprogram/pages/birding-spots/birding-spots.wxss` - 添加标签页相关的样式
- `miniprogram/utils/birdData.js` - 利用现有鸟类数据生成公园信息

## Task 1: 扩展页面数据结构

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.js:8-20`

- [ ] **Step 1: 在data中添加标签页相关状态**

```javascript
  data: {
    birdingSpots: [],
    selectedSpot: null,
    activeTab: 'overview',  // 添加活动标签页状态
    parkDetails: {
      overview: {},
      birds: {},
      ecology: {}
    },
    showParkDetails: false  // 控制详情面板显示
  },
```

- [ ] **Step 2: 验证修改**

检查文件是否正确更新了data结构，确认添加了tab相关的状态变量

- [ ] **Step 3: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.js
git commit -m "feat: add tab state variables to birding-spots page"
```

## Task 2: 扩展WXML结构以支持标签页

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.wxml:1-80`

- [ ] **Step 1: 在现有列表下方添加标签页导航和内容区域**

```xml
  <!-- 公园详情面板 -->
  <view class="park-details" wx:if="{{showParkDetails}}">
    <!-- 标签页导航 -->
    <view class="tab-nav">
      <view class="tab-item {{activeTab === 'overview' ? 'active' : ''}}" bindtap="switchTab" data-tab="overview">
        <text>概览</text>
      </view>
      <view class="tab-item {{activeTab === 'birds' ? 'active' : ''}}" bindtap="switchTab" data-tab="birds">
        <text>鸟类</text>
      </view>
      <view class="tab-item {{activeTab === 'ecology' ? 'active' : ''}}" bindtap="switchTab" data-tab="ecology">
        <text>生态环境</text>
      </view>
    </view>

    <!-- 标签页内容 -->
    <view class="tab-content">
      <!-- 概览标签内容 -->
      <view class="tab-pane" wx:if="{{activeTab === 'overview'}}">
        <view class="overview-section">
          <text class="section-title">{{selectedSpot.name}}</text>
          <view class="info-item">
            <text class="info-label">位置：</text>
            <text class="info-value">{{selectedSpot.location}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">面积：</text>
            <text class="info-value">{{selectedSpot.area || '未知'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">开放时间：</text>
            <text class="info-value">{{selectedSpot.openingHours || '详见公园公告'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">交通：</text>
            <text class="info-value">{{selectedSpot.transportation || '未提供'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">特色：</text>
            <text class="info-value">{{selectedSpot.features || '未提供'}}</text>
          </view>
        </view>
      </view>

      <!-- 鸟类标签内容 -->
      <view class="tab-pane" wx:if="{{activeTab === 'birds'}}">
        <view class="birds-section">
          <text class="section-title">常见鸟种</text>
          <view class="bird-list">
            <view class="bird-item" wx:for="{{selectedSpot.birds}}" wx:key="species">
              <text class="bird-name">{{item.commonName || item.species}}</text>
              <text class="bird-scientific">{{item.scientificName}}</text>
            </view>
          </view>
          <view class="info-item">
            <text class="info-label">最佳观鸟时间：</text>
            <text class="info-value">{{selectedSpot.bestBirdingTime || '未提供'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">观鸟热点：</text>
            <text class="info-value">{{selectedSpot.hotspots || '未提供'}}</text>
          </view>
        </view>
      </view>

      <!-- 生态环境标签内容 -->
      <view class="tab-pane" wx:if="{{activeTab === 'ecology'}}">
        <view class="ecology-section">
          <text class="section-title">生态环境</text>
          <view class="info-item">
            <text class="info-label">栖息环境：</text>
            <text class="info-value">{{selectedSpot.ecology.habitat || '未提供'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">植被类型：</text>
            <text class="info-value">{{selectedSpot.ecology.vegetation || '未提供'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">季节特征：</text>
            <text class="info-value">{{selectedSpot.ecology.seasonalFeatures || '未提供'}}</text>
          </view>
          <view class="info-item">
            <text class="info-label">保护状况：</text>
            <text class="info-value">{{selectedSpot.ecology.conservationStatus || '未提供'}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
```

- [ ] **Step 2: 验证WXML结构是否正确添加**

检查文件确认标签页结构已正确插入到适当位置

- [ ] **Step 3: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.wxml
git commit -m "feat: add tab structure to birding-spots page"
```

## Task 3: 添加标签页切换逻辑

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.js:80-150`

- [ ] **Step 1: 添加标签页切换方法**

```javascript
  /**
   * 切换标签页
   */
  switchTab(e) {
    const tab = e.currentTarget.dataset.tab;
    this.setData({
      activeTab: tab
    });
  },

  /**
   * 显示公园详情
   */
  showParkDetails(e) {
    const index = e.currentTarget.dataset.index;
    const spot = this.data.birdingSpots[index];
    
    // 从现有数据构建公园详情
    const parkDetails = this.buildParkDetails(spot);
    
    this.setData({
      selectedSpot: parkDetails,
      activeTab: 'overview',  // 默认显示概览
      showParkDetails: true
    });
  },

  /**
   * 隐藏公园详情
   */
  hideParkDetails() {
    this.setData({
      showParkDetails: false,
      selectedSpot: null
    });
  },

  /**
   * 从现有鸟类数据构建公园详情
   */
  buildParkDetails(spot) {
    // 从birdData中提取与此公园相关的鸟类
    const { realBirdObservations } = require('../../utils/birdData');
    const parkBirds = realBirdObservations.filter(bird => 
      bird.locationName.includes(spot.name) || 
      spot.name.includes(bird.locationName)
    );

    // 提取唯一鸟类品种
    const uniqueBirds = [];
    const speciesSet = new Set();
    parkBirds.forEach(bird => {
      if (!speciesSet.has(bird.species)) {
        uniqueBirds.push(bird);
        speciesSet.add(bird.species);
      }
    });

    // 构建公园详情对象
    return {
      ...spot,
      birds: uniqueBirds,
      area: spot.area || '未知',
      openingHours: spot.openingHours || '详见公园公告',
      transportation: spot.transportation || '未提供',
      features: spot.features || '未提供',
      bestBirdingTime: spot.bestBirdingTime || '春秋季最佳',
      hotspots: spot.hotspots || '未提供',
      ecology: {
        habitat: spot.ecology?.habitat || '未提供',
        vegetation: spot.ecology?.vegetation || '未提供',
        seasonalFeatures: spot.ecology?.seasonalFeatures || '未提供',
        conservationStatus: spot.ecology?.conservationStatus || '未提供'
      }
    };
  }
```

- [ ] **Step 2: 修改原有的 onSpotTap 方法来使用新的详情显示方式**

```javascript
  /**
   * 点击观鸟点
   */
  onSpotTap(e) {
    const index = e.currentTarget.dataset.index;
    this.showParkDetails(e);
  },
```

- [ ] **Step 3: 验证方法是否正确添加和修改**

检查文件确认所有方法都已正确添加到页面逻辑中

- [ ] **Step 4: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.js
git commit -m "feat: add tab switching and park details logic"
```

## Task 4: 添加标签页样式

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.wxss:1-150`

- [ ] **Step 1: 添加标签页相关样式**

```css
/* 公园详情面板样式 */
.park-details {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: white;
  z-index: 1000;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  padding-top: 20px;
  padding-bottom: 40px;
}

/* 标签页导航样式 */
.tab-nav {
  display: flex;
  height: 80rpx;
  background: #f5f5f5;
  border-bottom: 1px solid #e0e0e0;
}

.tab-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 80rpx;
  font-size: 28rpx;
  color: #666;
  position: relative;
  transition: all 0.3s ease;
}

.tab-item.active {
  color: #2e8b57;
  font-weight: bold;
}

.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 60rpx;
  height: 6rpx;
  background: #2e8b57;
  border-radius: 3rpx;
}

/* 标签页内容样式 */
.tab-content {
  flex: 1;
  overflow-y: auto;
  padding: 20rpx;
}

.tab-pane {
  min-height: 100%;
}

/* 内容区域样式 */
.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 30rpx;
  display: block;
}

.info-item {
  display: flex;
  margin: 20rpx 0;
  font-size: 28rpx;
  line-height: 1.6;
}

.info-label {
  color: #666;
  min-width: 160rpx;
  flex-shrink: 0;
}

.info-value {
  color: #333;
  flex: 1;
  word-wrap: break-word;
}

.overview-section, .birds-section, .ecology-section {
  background: #fff;
  border-radius: 12rpx;
  padding: 30rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.1);
}

.bird-list {
  margin: 20rpx 0;
}

.bird-item {
  padding: 15rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.bird-item:last-child {
  border-bottom: none;
}

.bird-name {
  font-weight: bold;
  color: #333;
  display: block;
}

.bird-scientific {
  font-size: 24rpx;
  color: #666;
  display: block;
  font-style: italic;
}
```

- [ ] **Step 2: 验证样式是否正确添加**

检查文件确认所有样式都已正确添加

- [ ] **Step 3: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.wxss
git commit -m "feat: add tab styles for park details"
```

## Task 5: 修改数据加载逻辑以支持公园详情

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.js:30-70`

- [ ] **Step 1: 修改 onLoad 方法以添加公园数据字段**

```javascript
  onLoad(options) {
    // 加载观鸟点数据
    this.setData({
      birdingSpots: [
        {
          id: 1,
          name: "深圳湾公园",
          location: "深圳市南山区望海路1408号",
          description: "深圳湾公园是观鸟胜地，每年冬季都有大量候鸟在此栖息，包括黑脸琵鹭、白鹭等多种珍稀鸟类。",
          area: "120公顷",
          openingHours: "6:00-23:00",
          transportation: "地铁2号线海上世界站A出口，步行约15分钟",
          features: "沿海湿地，红树林生态系统",
          bestBirdingTime: "秋冬季最佳，特别是11月至次年3月",
          hotspots: "北湾、中湾及南湾沿线，以及红树林区域",
          ecology: {
            habitat: "滨海湿地生态系统",
            vegetation: "红树林、芦苇、海草床",
            seasonalFeatures: "冬季候鸟天堂，夏秋两季也有留鸟和繁殖鸟",
            conservationStatus: "国家级重要生态区域"
          }
        },
        {
          id: 2,
          name: "华侨城国家湿地公园",
          location: "深圳市南山区白石路东8号",
          description: "华侨城湿地公园是深圳首个国家级湿地公园，拥有丰富的鸟类资源和优美的自然景观。",
          area: "68.5公顷",
          openingHours: "9:00-17:00（周一闭馆）",
          transportation: "地铁1号线侨城东站B出口，步行约10分钟",
          features: "淡水湿地，人工修复生态系统",
          bestBirdingTime: "全年适宜，春秋迁徙季节最佳",
          hotspots: "观鸟屋、水禽湖及周边芦苇荡",
          ecology: {
            habitat: "淡水湿地生态系统",
            vegetation: "芦苇、香蒲、荷花等水生植物",
            seasonalFeatures: "四季分明，春秋季候鸟迁徙高峰期",
            conservationStatus: "国家重要湿地，生态修复示范区"
          }
        },
        {
          id: 3,
          name: "福田红树林自然保护区",
          location: "深圳市福田区红树林路1号",
          description: "福田红树林自然保护区是全国唯一位于市区的国家级自然保护区，鸟类资源极为丰富。",
          area: "367.64公顷",
          openingHours: "9:00-17:00",
          transportation: "地铁9号线侨城东站B出口，步行约15分钟",
          features: "天然红树林，生物多样性极高的保护区",
          bestBirdingTime: "10月至次年4月最佳",
          hotspots: "基围、高潮位栖息地、核心区域",
          ecology: {
            habitat: "天然红树林生态系统",
            vegetation: "秋茄、桐花树、海桑等红树植物",
            seasonalFeatures: "冬季候鸟高峰，春秋季迁徙期",
            conservationStatus: "国际重要湿地，拉姆萨尔公约名录"
          }
        },
        {
          id: 4,
          name: "仙湖植物园",
          location: "深圳市罗湖区莲塘仙湖路160号",
          description: "仙湖植物园不仅植物种类丰富，也是观鸟的好去处，尤其在迁徙季节能看到多种林鸟。",
          area: "668公顷",
          openingHours: "8:00-18:00",
          transportation: "梧桐山假日专线1号、M445路公交车仙湖植物园站",
          features: "山地森林生态系统，植物多样性丰富",
          bestBirdingTime: "春秋迁徙季节，早晨和傍晚最佳",
          hotspots: "苏铁园、竹园及森林步道",
          ecology: {
            habitat: "亚热带山地森林生态系统",
            vegetation: "苏铁、竹类、蕨类及多种乔灌木",
            seasonalFeatures: "迁徙鸟类中转站，四季鸟种不同",
            conservationStatus: "保护珍稀植物与鸟类栖息地"
          }
        },
        {
          id: 5,
          name: "东湖公园",
          location: "深圳市罗湖区爱国路4006号",
          description: "东湖公园是深圳市最早的市政公园之一，水域面积较大，是城市中的鸟类栖息地。",
          area: "151公顷",
          openingHours: "6:00-22:00",
          transportation: "地铁14号线黄木岗站A出口，步行约10分钟",
          features: "城市综合性公园，水系发达",
          bestBirdingTime: "全年适宜，清晨最佳",
          hotspots: "湖心岛、亲水平台及湖岸区域",
          ecology: {
            habitat: "城市淡水湖泊生态系统",
            vegetation: "湖滨植物、绿化带树木",
            seasonalFeatures: "留鸟为主，春秋有少量迁徙鸟",
            conservationStatus: "城市生态绿肺，鸟类栖息地"
          }
        }
      ]
    });
  },
```

- [ ] **Step 2: 验证数据结构是否已更新**

检查文件确认所有公园数据都已包含扩展字段

- [ ] **Step 3: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.js
git commit -m "feat: extend birding spots data with park details"
```

## Task 6: 添加返回按钮以关闭详情面板

**Files:**
- Modify: `miniprogram/pages/birding-spots/birding-spots.wxml:20-40`

- [ ] **Step 1: 在公园详情面板顶部添加返回按钮**

```xml
  <!-- 公园详情面板 -->
  <view class="park-details" wx:if="{{showParkDetails}}">
    <!-- 顶部工具栏 -->
    <view class="top-toolbar">
      <view class="back-btn" bindtap="hideParkDetails">
        <text class="back-icon">‹</text>
        <text class="back-text">返回</text>
      </view>
    </view>
    
    <!-- 标签页导航 -->
    <view class="tab-nav">
```

- [ ] **Step 2: 添加返回按钮的样式**

```css
/* 顶部工具栏样式 */
.top-toolbar {
  height: 80rpx;
  display: flex;
  align-items: center;
  padding: 0 20rpx;
  background: white;
  border-bottom: 1px solid #e0e0e0;
}

.back-btn {
  display: flex;
  align-items: center;
  padding: 10rpx 20rpx;
  border-radius: 30rpx;
  background: #f5f5f5;
}

.back-icon {
  font-size: 36rpx;
  color: #666;
  margin-right: 10rpx;
}

.back-text {
  font-size: 28rpx;
  color: #333;
}
```

- [ ] **Step 3: 验证返回按钮是否已添加**

检查文件确认返回按钮已正确添加

- [ ] **Step 4: 提交更改**

```bash
git add miniprogram/pages/birding-spots/birding-spots.wxml miniprogram/pages/birding-spots/birding-spots.wxss
git commit -m "feat: add back button to park details panel"
```